import {makeUser} from "../../models";
import {UserInfos} from "../../types";
import {makeDb} from "../";

export default function makeAddUser({}) {
    return async function addUser(userInfos:UserInfos) {
        return;
    }
}